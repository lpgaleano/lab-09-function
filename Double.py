def double(n):
        # n is a parmeter that is used within the function definition.
        # parmeter are a placeholder for input data that gets processed by our functions
        return n * 2

userstring = input("number please...")
usernum = int(userstring)

print(double(usernum));
          # usernum is passed as an argument to the double () function call
          #an argument is a placeholder for a real value that gets passed into a function call {
          # line 18 order : 1 usernum is evaluated 2 double () is evaluated 3 console.log is evaluated
